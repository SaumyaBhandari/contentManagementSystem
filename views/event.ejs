<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./head'); %>
    <link rel="stylesheet" href="/css/error.css" />
  </head>
  <body>
    <%- include('./header'); %> <% if (event) {%>

    <section class="card-section mt-4">
      <div class="card-group">
        <div class="card m-4">
          <div class="card-body m-3">
            <div class="mr-3">
              <h3 class="mt-3 mb-3 ml-4 text-justify">
                <%= event.noticeType%>
              </h3>
              <h5 class="mt-3 mb-3 ml-4 text-justify"><%= event.name %></h5>

              <p class="ml-4 text-justify">
                <%= event.description %>
                <br /><br />

                <img
                  src="/images/events/<%= event.coverPhoto %>"
                  class="mt-2 ml-4"
                />
              </p>

              <h6 class="text-justify mt-5 mb-1 ml-4">
                Event Date : <%= event.startDate.toLocaleDateString('en-US',{
                year: 'numeric', month: 'short', day: 'numeric' }) %>
              </h6>
              <h6 class="text-justify mt-1 mb-3 ml-4">
                Location of event : <%= event.location.toUpperCase() %>
              </h6>
            </div>
            <a href="/events" class="float-right">back to events</a>
          </div>
        </div>
      </div>
    </section>

    <%}%>

    <footer><%- include('./footer'); %></footer>
  </body>
</html>
